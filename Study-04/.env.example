# 이 파일은 "양식"이에요. 실제 키는 넣지 마세요 (GitHub에 올라갑니다).
# 로컬 환경에서 쓸 때만: 이 파일을 복사해서 이름을 .env 로 바꾸고, 아래에 진짜 키를 넣으면 됩니다.
# Colab에서는 .env 대신 왼쪽 열쇠 아이콘(보안 비밀)에 키를 넣어 쓰세요 (.env 필요 없음).
OPENROUTER_API_KEY=여기에_발급받은_키
